<article>
  <header>
    <h1><a href="{{ post.url }}">{{ post.data.title }}</a></h1>
  </header>
  <p class="excerpt">{{ post.excerpt | safe }}</p>
  <footer>
    <p data-tags>{{ post.data.tags | join(", ") }}</p>
    <p><time>{{ post.date.toLocaleDateString() }}</time></p>
  </footer>
</article>
